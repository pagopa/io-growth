meta {
  name: Assertion Consumer Service callback
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/acs
  body: none
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{tokenAdminTest}}
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("sessionId", res.body.sessionId, { persist: false });
  }
}
