{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputs": []
    },
    "migrate:dev": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "migrate:prod": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "run:dev": {
      "dependsOn": ["^build"],
      "persistent": true,
      "cache": false
    },
    "typecheck": {
      "inputs": [
        "src/**/*.ts",
        "package.json",
        "tsconfig.json",
        "!vitest.config.*"
      ],
      "outputs": [],
      "dependsOn": ["^build", "^typecheck"]
    },
    "lint": {
      "inputs": ["src/**/*.ts"]
    },
    "lint:check": {
      "inputs": ["src/**/*.ts"]
    },
    "format:check": {},
    "test": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputLogs": "full"
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputs": ["coverage"],
      "outputLogs": "full"
    }
  }
}
