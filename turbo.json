{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate": {
      "inputs": ["openapi.yaml", "orval.config.ts"],
      "outputs": ["src/adapters/inbound/fastify/contracts/**"]
    },
    "build": {
      "dependsOn": ["^build", "generate"],
      "inputs": ["src/**", "tsconfig.json", "package.json"],
      "outputs": ["dist/**"]
    },
    "migrate:dev": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "migrate:prod": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "run:dev": {
      "dependsOn": ["^build", "generate"],
      "persistent": true,
      "cache": false
    },
    "typecheck": {
      "inputs": [
        "src/**/*.ts",
        "package.json",
        "tsconfig.json",
        "!vitest.config.*"
      ],
      "outputs": [],
      "dependsOn": ["^build", "^typecheck", "generate"]
    },
    "lint": {
      "inputs": ["src/**/*.ts"]
    },
    "lint:check": {
      "inputs": ["src/**/*.ts"]
    },
    "format:check": {},
    "test": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputLogs": "full"
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": [],
      "outputs": ["coverage"],
      "outputLogs": "full"
    }
  }
}
