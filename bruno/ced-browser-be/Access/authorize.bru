meta {
  name: Exchange session id for a session token
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/authorize
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("sessionToken", res.body.token);
  }
}
