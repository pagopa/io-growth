---
"@pagopa/io-core-ui": minor
---

Render `Body` as a real button when used as a link with an `onClick`, so it is reachable by keyboard and shows a focus ring. Without `onClick` it stays plain text with the link styling, so an already-interactive parent can wrap it without nesting two interactive elements.
